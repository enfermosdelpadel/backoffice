import Layout from "../../Components/Layout";

const Home = () => {
  // const context = useContext(TaskContext);

  return (
    <Layout>
      <div>
        <h1>Welcome Baby!</h1>
      </div>
    </Layout>
  );
};

export default Home;
